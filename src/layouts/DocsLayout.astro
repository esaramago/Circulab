---
import Layout from '@/layouts/Layout.astro'
const docs = Object.values(import.meta.glob('../pages/[[]...lang]/docs/*.md', { eager: true }))
const orderedDocs = docs.sort((a: any, b: any) => a.frontmatter.order - b.frontmatter.order)
---

<Layout title={Astro.props.title}>
  <h1>{Astro.props.title}</h1>
  <nav>
    <ul>
      {
        orderedDocs.map((doc: any) => (
          <li>
            <a href={doc.url.replace('/[...lang]', '')}>{doc.frontmatter.title || doc.url}</a>
          </li>
        ))
      }
    </ul>
  </nav>
  <section>
    <slot />
  </section>
</Layout>