---
import Layout from '@/layouts/Layout.astro'
import Wizard from '@/components/pages/add/Wizard.vue'
import WizardStep from '@/components/pages/add/WizardStep.vue'

const steps = [
  {
    code: 'validation',
    label: 'Validação'
  },
  {
    code: 'description',
    label: 'Descrição'
  },
  {
    code: 'characterization',
    label: 'Caracterização'
  },
  {
    code: 'location',
    label: 'Localização'
  },
  {
    code: 'summary',
    label: 'Resumo'
  }
]

const currentStep = Astro.props.currentStep || 'validation'
---

<Layout title="Adicionar">
  <h1>Adicionar pin ao mapa</h1>
  <Wizard>
    {
      steps.map((step, index) => (
        <WizardStep client:only="vue" step={index + 1} label={step.label} isActive={step.code === currentStep}></WizardStep>
      ))
    }
  </Wizard>
  <slot />
</Layout>