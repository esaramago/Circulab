---
import Layout from '@/layouts/Layout.astro'
import { m } from '@/paraglide/messages.js'
---

<Layout title={m['about.title']()}>
  <h1>{m['about.title']()}</h1>
  <p>{m.site_description()}</p>
  <p>{Astro.currentLocale}</p>
</Layout>